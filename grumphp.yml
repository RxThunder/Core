parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcsfixer2:
            config: .php_cs.dist
        phpstan:
            level: 7
            ignore_patterns: ['/^(?!src).*/']
        securitychecker:
